#include <stdio.h>
#include <stdlib.h>

int main()
{
    char name[100];

     printf ("Enter your name with your father name,address\n");
     gets(name);
     printf ( "You have entered the details is given below\n");
     //printf ("%s",name);
      puts(name);
    return 0;
}
